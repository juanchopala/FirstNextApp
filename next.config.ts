import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  /*
  experimental: {
  ppr: 'incremental'
  }
  comentado por que sino lo hago el projecto no me anda 
  */
};

export default nextConfig;
